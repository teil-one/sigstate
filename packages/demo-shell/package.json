{
  "name": "demo-shell",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rsbuild build",
    "dev": "rsbuild dev",
    "format": "prettier --write .",
    "lint": "eslint .",
    "preview": "rsbuild preview"
  },
  "dependencies": {
    "@teil-one/sigstate": "workspace:*"
  },
  "devDependencies": {
    "@rsbuild/core": "1.0.1-beta.14"
  }
}
