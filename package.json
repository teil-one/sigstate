{
  "name": "sigstate",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"bun run build:watch\" \"bun run --cwd ./packages/demo-shell dev\" \"bun run --cwd ./packages/demo-cart dev\"",
    "build": "bun run --cwd ./packages/sigstate build && bun run --cwd ./packages/sigstate-preact-adapter build",
    "build:watch": "concurrently \"bun run --cwd ./packages/sigstate build --watch\" \"bun run --cwd ./packages/sigstate-preact-adapter build --watch\"",
    "format": "prettier --write .",
    "lint": "eslint ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "prettier": "^3.3.3",
    "typescript-eslint": "^8.0.1"
  }
}
